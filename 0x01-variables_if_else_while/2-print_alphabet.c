#include <stdio.h>

/*
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char letter;
	for (letter = 'a'; letter <= 'z': letter++)
		putcher(letter);
	putcher('\n');

	return (0);
}
